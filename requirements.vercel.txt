# Minimal Vercel deployment - FastAPI + basic utilities only
# Full ML dependencies (torch, transformers, etc.) stay on AWS EC2
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
prometheus-client>=0.19.0
sentry-sdk>=1.40.0
python-multipart>=0.0.6
httpx>=0.27.0
