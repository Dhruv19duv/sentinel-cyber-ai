{
  "version": 2,
  "functions": {
    "api/index.py": {
      "memory": 256,
      "maxDuration": 10
    }
  },
  "routes": [
    { "src": "/(.*)", "dest": "api/index.py" }
  ],
  "installCommand": "pip install --no-cache-dir -r requirements.vercel.txt",
  "env": {
    "SENTINEL_CORS_ORIGINS": "*",
    "SENTINEL_LOG_LEVEL": "INFO",
    "SENTINEL_PLATFORM": "vercel"
  }
}
