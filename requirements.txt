# Core ML
torch>=2.2.0
transformers>=4.45.0
datasets>=2.18.0
accelerate>=0.28.0
peft>=0.11.0
trl>=0.8.0,<0.11.0

# Unsloth (optimized QLoRA training for large models)
# pip install "unsloth[cu126] @ git+https://github.com/unslothai/unsloth.git"

# API Server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
python-multipart>=0.0.12

# Utilities
sentencepiece>=0.2.0
protobuf>=3.20.3
bitsandbytes>=0.43.0
scipy>=1.12.0
pyyaml>=6.0

# Dataset processing
requests>=2.31.0
jsonlines>=4.0.0

# Tokenization
tokenizers>=0.19.0

# Evaluation
evaluate>=0.4.1
psutil>=5.9.0

# Error Tracking & Performance Monitoring
sentry-sdk>=2.18.0
prometheus-client>=0.21.0

# IDE Bridge (WebSocket server for VS Code/JetBrains integration)
websockets>=12.0

# Optional: for CodeQL/semgrep integration
# codeql           # Install separately from GitHub
# semgrep>=1.50.0  # pip install semgrep
